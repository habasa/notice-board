import React from 'react';

const UpdateBtn = () => {
    return (
        <div>
            <button className='delete-button'
            >수정</button>
        </div>
    );
};

export default UpdateBtn;